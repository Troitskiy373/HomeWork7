//Создание двумерного массива
/*int[,] CreatyRandomArray(int m, int n, int minValue, int maxValue)
{
    int[,] newArray = new int[m, n];

    for (int i = 0; i < m; i++)
        for (int j = 0; j < n; j++)
            newArray[i, j] = new Random().Next(minValue, maxValue + 1);
    return newArray;
}

//Показать массив
void Show2dArray(int[,] array)
{
    for (int i = 0; i < array.GetLength(0); i++)//GetLength(0)количество строк(длина)
    {
        for (int j = 0; j < array.GetLength(1); j++)//array.GetLength(1) количество столбцов(длина)
            Console.Write(array[i, j] + " ");

        Console.WriteLine();
    }
}


Console.Write("imput m : ");
int m = Convert.ToInt32(Console.ReadLine());

Console.Write("imput n : ");
int n = Convert.ToInt32(Console.ReadLine());

int[,] myArray = CreatyRandomArray(m, n, 1, 9);

Show2dArray(myArray);*/


// Задача 


